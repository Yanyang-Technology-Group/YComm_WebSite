# dependencies
node_modules/
# npm cache is kept inside the repo so installs work under a restricted sandbox
.npm-cache/

# build output
.next/
out/
dist/
build/
*.tsbuildinfo
next-env.d.ts

# local data (PGlite dev database, uploads)
.data/
uploads/

# env & local overrides
.env
.env.local
.env.*.local

# logs & coverage
*.log
npm-debug.log*
coverage/

# editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
# ---- 本地机密与临时文件（严禁提交） ----
.deploy-key*
.secrets.txt
.dbpass.txt
.openapi.json
.appuuid*.txt
.genkey.mjs
.verify.mjs
.redir-app.txt
.spdx-lic/
.agpl-full.txt
# 本地 SSH 验证工具目录（含转换后的私钥副本，绝不入库）
.tmpssh/
# 本地部署脚本（含服务器/数据库机密，绝不入库）
/deploy.sh
/deploy.bat
# 手动触发布署脚本（含 Coolify API token，绝不入库）
/redeploy.sh
/redeploy.bat
# 本地一次性数据/导入脚本（含用户邮箱等隐私，绝不入库）
/private/
# 站点图标的本地源目录（成品已入库：apps/web/public/logo.png + apps/web/src/app/icon.png）
/logo/

# 本地运维辅助脚本（创建下载区卡片用；按用户要求不入库，只留本地）
/scripts/create-app-cards.ps1
/scripts/create-app-cards.cmd
